# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Target directory
target/

# Local databases (generated by SQLite)
*.db

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
